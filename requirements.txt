# Зафиксированные версии проверены на Python 3.11 и 3.14.
openpyxl==3.1.5
python-calamine==0.6.1
colorama==0.4.6
reflex==0.9.8
starlette==1.6.0

# Only for optional invoice OCR: validates images and converts WEBP before Yandex Vision.
Pillow==12.1.1

# Only for optional chat assistant (floating corner button) — the rest of the dashboard does not depend on it.
anthropic==1.3.0
