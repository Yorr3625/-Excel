.web
*.db
*.py[cod]
assets/external/
.states
__pycache__/
*.pyc

build/
dist/
*.spec

# Accidental shell/home artifacts
~/

# Reflex (веб-дашборд orders_dashboard)
.web/
reflex.lock/
.states/

# Рабочие данные: заказы, результаты, логи, история дашборда.
# Файлы внутри не версионируются, но сама папка нужна приложению.
data/*
!data/.gitkeep

# Настройки почты — в них пароль приложения. В git только шаблон.
config/mail.json
config/backup.json

# Ignore Excel files
*.xlsx
*.xls
*.xlsm
*.xlsb
*.xltx
*.xltm

venv/
.venv/

# Скачивается автоматически share_dashboard.bat (утилита временного туннеля)
tools/

.pytest_cache/

.vscode/
.idea/
# Claude Code: keep shared project instructions, ignore local settings
.claude/*
!.claude/
!.claude/settings.json

Thumbs.db
.DS_Store
